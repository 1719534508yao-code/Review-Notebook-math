# 窗口指令 —— 按顺序开窗口，整段复制粘贴

> 用法：在项目目录 `/Users/yao/ai程序库/试卷集` 下每开一个新的 Claude Code 窗口，
> 把对应「窗口 N」代码块里的全部文字复制粘贴进去即可。
> **顺序**：1 → 2 → 3 → 4 必须串行；窗口 4 完成后，5 / 6 / 7 可并行开三个窗口同时跑；最后窗口 8 总验收。
> 每个窗口做完都会更新 PROGRESS.md，下一个窗口会先读它，不会重复劳动。

---

## 窗口 1 —— T1 地基

```
你是本项目的开发 agent。项目唯一事实来源是 /Users/yao/ai程序库/试卷集/DESIGN.md，先完整阅读它的 §2（需求决策）、§4（项目结构）、§5 和 §5.5（数据模型与归档布局）、§6-T1（本次任务规格）。若 PROGRESS.md 存在也先读。

本窗口只做 T1 地基，不要实现切片、AI 打标、统计页面（那是后续 agent 的任务）：
1. requirements.txt（兼容本机 Python 3.9 并钉死版本）、run.sh（venv+首次装依赖+启动 uvicorn+自动开浏览器，chmod +x）、.env.example、README.md 骨架
2. backend/models.py + db.py：按 §5 建全部表和视图（幂等）；预置 subject「数学」、§2-13 用户给定的 9 个题型、北京 18 区常量
3. data/kp_math.json：按 §6-T1 写 10 大类约 90 叶子的高考数学知识点树；backend/kp_seed.py 幂等导入（parent+name 判重）
4. app.py + backend/api.py：/api/health、/api/kp/tree、/api/meta/enums 三个路由；static/index.html 先放占位页

验收（做完必须在回复里贴出实际执行结果）：./run.sh 能起服务；curl /api/health 返回 200；/api/kp/tree 返回完整种子树；重启一次不重复导入。
完成后新建 PROGRESS.md，记录：T1 完成、实际文件清单、与 DESIGN.md 的任何偏离或你补充的决定。
```

## 窗口 2 —— T2 PDF 切片

```
你是本项目的开发 agent。先完整阅读 DESIGN.md 的 §2、§5.5、§6-T2，再读 PROGRESS.md（T1 已完成，复用它的 db 和骨架，不要动其他模块）。

本窗口只做 T2：backend/pdf_slicer.py。核心要求：
- PyMuPDF 文字块坐标定位题号（正则+递增校验防误判）、双栏版面分栏排序、跨页接续
- 单题跨两页：两个 rect 各渲染 2x PNG，Pillow 纵向拼接成一张完整切片（§6-T2 第 3 条），raw_text 跨页合并
- 切片输出到 storage/crops/{exam_id}/{num}.png（中间态，先不动 archive）
- 未识别的题号在返回值里标 unlocated，供复核页提示

先向我要 1~2 套真实试卷 PDF 的路径来调参；拿到之前先生成一个双栏+含跨页题的测试 PDF 做开发用例。
验收：测试用例 + 真实卷目测 22 题边界正确、跨页题拼接完整不乱序。回复里贴运行输出和切片文件列表。
完成后更新 PROGRESS.md（T2 状态+偏离）。
```

## 窗口 3 —— T3 上传/复核

```
你是本项目的开发 agent。先完整阅读 DESIGN.md 的 §2、§5、§5.5、§6-T3，再读 PROGRESS.md（T1/T2 已完成，只做本任务，不重写它们的模块）。

本窗口只做上传与复核全链路：
- 后端路由：POST /api/exams（multipart+手选 年/区/型，调 T2 切片，保留源 PDF 待确认）、GET /api/exams/{id}/questions、PATCH /api/questions/{id}、POST /api/exams/{id}/confirm（confirm 成功后：删源 PDF；正确题切片复制入 archive/数学/题库/{年}/{区}/{型}/{题号}.png；触发打标任务接口先留桩）
- 前端 static/：上传 view（三下拉记住上次选择）+ 复核 view（切片网格、点击红框标错题、9 类题型下拉、确认按钮）。原生 HTML/JS，无构建工具
验收：上传真实卷→点错 3 题→确认→源 PDF 已删、题库目录出现未打标切片、DB 状态正确。回复贴出结果。
完成后更新 PROGRESS.md。
```

## 窗口 4 —— T4 AI 打标

```
你是本项目的开发 agent。先完整阅读 DESIGN.md 的 §2（尤其决策 5、6、8、13）、§5.5、§6-T4，再读 PROGRESS.md（T1-T3 已完成，只做本任务）。

本窗口只做 backend/tagger.py 及对应路由：
- DashScope OpenAI 兼容端点 + qwen-vl-max；输入切片 PNG(base64)+raw_text+紧凑知识点树+9 题型；严格 JSON 输出并代码校验，规则见 §6-T4（含混合题型判定规则、主/次知识点、new_kp 建议）
- 无 DASHSCOPE_API_KEY 时的「手动模式」降级是硬性要求（明确文案、PATCH 可手填 kp/qtype）
- 每题打标成功即执行 §6-T4「归档落地」：错题移入 archive/数学/错题本/{题型}/{年}{区}{型}_{题号}.png；正确题改名为 {题号}_{题型}.png；image_path 更新
- 新知识点建议的 approve/reject 路由（approve 建叶子+回填主标签）
- 前端：确认横幅（建议新增知识点一键确认）、识别进度显示

先检查 .env 是否有 key 并问我；有则用真实卷（复用 T3 那套）跑完整识别做验收，无则验收手动模式+mock 打标路径。回复贴出实际结果与单套卷 token 消耗估算。
完成后更新 PROGRESS.md。
```

## 窗口 5 —— T5 错题本/题库浏览（4 完成后可与 6、7 并行）

```
你是本项目的开发 agent。先完整阅读 DESIGN.md 的 §2、§5、§6-T5，再读 PROGRESS.md（T1-T4 已完成；只做本任务，不动 T6/T7 范围）。

本窗口只做错题本与题库浏览：
- GET /api/wrong_bank、GET /api/pool：按 subject/知识点/题型/区/考试类型/日期筛选+分页，返回 image_path 与主次标签
- 前端两个 view（共用列表组件）：卡片=切片图+题号+出处+题型+主知识点标签，点开看大图
- 图片静态服务需同时覆盖 /archive/ 路径
验收：T4 那套卷的错题只出现在错题本、其余只出现在题库；按知识点筛选数得对（与 DB count 比对）。若此时没有 tagged 数据，用 SQL 造 mock 数据验证并说明。回复贴结果。
完成后更新 PROGRESS.md。
```

## 窗口 6 —— T6 气泡图（可与 5、7 并行）

```
你是本项目的开发 agent。先完整阅读 DESIGN.md 的 §2（决策 6、8）、§6-T6，再读 PROGRESS.md（T1-T4 已完成；只做本任务）。

本窗口只做知识点气泡图：
- GET /api/stats/bubbles：叶子+小类聚合 {done, wrong, error_rate}，统计只数 primary 标签（§5.5 规则），支持 区/类型/时间 筛选
- 前端 D3 force-pack 视图：大小=题数、颜色=错误率绿黄红连续色带、大类成簇；hover tooltip；点叶子抽屉列错题缩略图
- d3.v7.min.js 下载一次放入 static/vendor/（本地引用，不许用运行时 CDN）
验收：构造或复用 tagged 数据，气泡数字与 /api/pool、错题本列表条数一致；次标签确认不计数。回复贴比对结果。
完成后更新 PROGRESS.md。
```

## 窗口 7 —— T7 周报（可与 5、6 并行）

```
你是本项目的开发 agent。先完整阅读 DESIGN.md 的 §2（决策 11）、§5.5、§6-T7，再读 PROGRESS.md（T1-T4 已完成；只做本任务）。

本窗口只做每周总结：
- GET /api/stats/weekly：近 7 天按知识点聚合、错误率 top3（样本≥3）、各题型正确率、规则化下周题型推荐（纯 SQL/Python，不用 LLM）
- 生成同时写 archive/数学/计划库/周报-{起}-{止}.md（重生成覆盖）
- 前端「周报」view：读取 md 渲染（marked 本地 vendored）+ 建议卡片
验收：改 created_at 造上周数据，验证时间窗/门槛/排序；确认 md 文件真实落盘且界面与文件一致。回复贴结果。
完成后更新 PROGRESS.md。
```

## 窗口 8 —— 总验收与修补（最后跑）

```
你是本项目的验收 agent。完整阅读 DESIGN.md（全文）和 PROGRESS.md，按 DESIGN.md §7 的端到端清单逐项实际执行：
./run.sh → 用真实卷上传 → 手选元信息 → 复核点错 → AI 识别 → 到 Finder 层核对 archive/（源 PDF 已删、错题本/题库路径与文件名正确）→ 错题本/题库/气泡图/周报四个 view 逐一检查数字一致 → 再全程走一遍无 key 手动模式。
发现问题就地修复（修复记录写回 PROGRESS.md），最后把 README.md 补成给用户的完整使用说明（启动、上传流程、API key 配置、归档目录说明）。回复里贴逐项验收结果表。
```

---

### 备注
- 每个窗口开头那句「先读 DESIGN.md + PROGRESS.md」是 agent 之间唯一的接力棒，不要删。
- 如果某个 agent 说「与设计冲突」，以 DESIGN.md §2 决策表为准；确实要改设计的，让它把改动理由写进 PROGRESS.md 再动。
- 窗口 2 和 4 需要你在场（要给试卷路径 / API key）；其余窗口可以挂着让它自己跑。
