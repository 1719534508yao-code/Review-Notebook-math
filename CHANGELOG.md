# 更新日志 CHANGELOG

> **这是什么**：这个项目从头到现在，每增加一个功能、每改一处东西，都在这里记一笔。
> 写给**任何接手的人**看——包括下次打开这个项目的我自己。
>
> **怎么用**：新会话开工前，先读最上面的「当前状态与待办」，再看下面的变更记录。
> 每一节都写清四件事：**改了什么功能 / 动了哪些文件 / 为什么这么改 / 实测结果如何**。
>
> **基线**：2026-09-16 项目总验收通过那一刻，记为 **v1.0**。本文件从那里开始记。
> **配套**：`PROGRESS.md` 是写给下一个开发窗口的技术接力棒（含详细技术论证），
> 本文件是写给任何人的变更史。两个都要看。
>
> **约定**：以后每完成一个功能或改动，**同一轮里**就在本文件追加一节，不要等别人来问。

---

# 📌 当前状态与待办（开工前先看这里）

## ✅ 已完成

| 模块 | 状态 |
|---|---|
| T1 地基（表结构 / 启动脚本 / 预置数据） | ✅ |
| T2 PDF 切片（按题号自动切图） | ✅ |
| T3 上传 → 复核 → 确认 → 归档 全链路 | ✅ |
| T4 AI 打标（视觉模型认题 → 判题型 + 知识点 → 自动归档） | ✅ 可用，但知识点准确率不够 |
| T5 错题本 / 题库浏览与筛选 | ✅ |
| T6 知识点气泡图 | ✅ |
| T7 周总结与推荐 | ✅ |
| 窗口 8 总验收（DESIGN §7 逐项实跑） | ✅ 通过 |
| 窗口 9 首页汇总树 + 删除单题 | ✅ |
| 窗口 10 数学卷 21 题规则 + 删除整套卷 | ✅ |
| T4 续：真实 key 实测 / 评测基建 / 准确率优化 / 模型 A/B / 降本专项 | ✅ |

## ⏳ 未完成 —— 以及「继续该怎么做」

> 按优先级排。**第 1 条是用户 2026-09-17 明确要的功能，方案已定稿但一行代码都没动。**

### 1. 【未开始】知识点库自定义 + 判别线索 ← 用户最新要求

**要做的事**：让用户能把自己那套知识点清单粘贴进系统，AI 按他的清单来判，
并且每条知识点可以带一句「判别线索」（例如「极值与最值 :: 求 f'(x)=0 后比较驻点与端点」）。

**方案已写好在哪**：`/Users/yao/.claude/plans/indexed-napping-dragon.md`

**继续该怎么做**（按顺序）：
1. **先保命**：`cp storage/app.db storage/app.db.bak-$(date +%s)`；
   然后 `git add -A && git commit`（项目有 `.git` 但**零 commit**，现在没有任何回滚点）。
2. **恢复测试环境**（见下面「⚠️ 环境现状」第 1 条）——不然改完没东西可验证。
3. **改 schema**：给 `knowledge_points` 加三列 `hint` / `qtype_hint` / `disabled`
   （`backend/models.py` 的 DDL + `backend/db.py::init_db()` 里加一段用 `PRAGMA table_info`
   预检的迁移）。**不要**去改 `origin`/`status` 的 CHECK 约束——那要重建表，风险太大。
4. **改打标侧**：`backend/tagger.py` 的 `compact_kp_tree` 把线索拼进叶子行、过滤 `disabled`；
   `_build_stage2` 同步；`cats_leaves` 从二元组改三元组（**两处解包必须同批改完，漏一处会崩**）。
5. **保住题型准确率**：`CAT_TO_QTYPE` 硬编码了 6 个大类名，用户改大类名后会**静默失效**。
   改成「用户填的 `qtype_hint` → 内置表回落 → 都不中就明确报出来」。
6. **写导入模块** `backend/kp_import.py`（同名复用保留原 id；被删但有题目引用的改成「停用」不删）+ 粘贴页面 `static/kp.html`。
7. **量效果**——**必须先只加线索、不换树，量一次；再换整棵树，量第二次**。
   一次全改完，数字变了也不知道是谁的功劳。（命令见方案文件的「验证」一节）

**为什么这件事有价值**：实测诊断过，模型判错板块的只有 19%，但进了板块之后
**分不清是哪片叶子**——10 道错题里 6 道是「大类判对、叶子判错」。
它缺的不是「看懂题」，是「在 97 个近义叶子里挑对」的分辨力。判别线索正好补这个。

### 2. 【未开始】用户指令「用 4v flash 全程吧」还没落地

用户 2026-09-16 最后一条指令是改用 `glm-4v-flash` 跑全程。**`.env` 里现在仍是 `glm-4v-plus`。**

要继续就改 `.env` 的 `VLM_MODEL=glm-4v-flash`（`VLM_MODEL_STAGE1` 留空即两段都用 flash）。

**用户需要知道的取舍**（当时测得的数据）：

| 配置 | token/套 | 叶子级准确率 | 大类级 | 耗时 |
|---|---|---|---|---|
| `glm-4v-plus` 全程（当前） | 121,338 | **61.9%** | **90.5%** | 2:09 |
| **`glm-4v-flash` 全程（用户要的）** | **37,233（省 69%）** | **47.6%** | 76.2% | **0:55** |

即：**省 69% 的钱、快一倍多，但知识点叶子级从 61.9% 掉到 47.6%——超过一半要人工改。**
⚠️ 注意只有 21 道题，**1 道题就是 4.8 个百分点**，这个差距里有一部分是噪声。

### 3. 【未完成】金标准集需要用户复核

`data/gold/gold_exam1.json` 是**我根据题干文字草拟的，不是用户确认的**
（21 题，把握度 high=13 / mid=7 / low=1）。**目前所有准确率数字都建立在这份草稿上**，
mid/low 那些题本身有歧义，模型和人的分歧未必是模型错。

要继续：请用户过一遍这份文件（或打开 `http://127.0.0.1:8000/eval.html` 点选），
重点看 `_conf` 标了 mid/low 的 8 道题。复核完准确率数字才算定论。

### 4. 【长期】知识点准确率不足以无人值守

叶子级 61.9%（高把握子集 84.6%），剩余 8 道错题：题 5/10/14/15/16/17/20/21。
`PROGRESS.md` §F 列了两条最值得做的：
(a) **自洽投票**（同题跑 2~3 次取多数，无多数则弃权）——
    把「答案不稳定」从 bug 变成置信信号，现在的弃权率是 0%，值得怀疑；
(b) **判别性线索** ← 就是上面第 1 条要做的事。

## ⚠️ 环境现状（要动手前必须先知道）

1. **主数据库 `storage/app.db` 现在是空的**（0 套卷、0 道题）。
   真正的 exam #1 数据在 `storage/test_weekly/real.db`，而且那份库里 `image_path`
   指向的 `archive/...` 文件**已经全部不存在了**（死链）。
   **好消息**：原卷 PDF 还在 `tests/2022北京朝阳高二（下）期末数学.pdf`，
   21 张切片也还在 `storage/crops/real_2022北京朝阳高二（下）期末数学/`。
   → **继续该怎么做**：写个一次性脚本把 exam #1 从 `real.db` 恢复到 `app.db`，
   并把 `image_path` 改指向现存切片、`status` 重置为 `untagged`、清空标签，
   得到一个可反复重跑打标的干净测试台。

2. **项目有 `.git`，但 main 分支零 commit**——所有文件停在 staged 初始态。
   等于**没有任何回滚点**。动手前第一件事就是建基线 commit。

3. **没有自动化测试框架**。现有的是几个手写脚本：
   `tests/test_pdf_slicer.py` / `test_bubbles.py` / `test_weekly.py` / `eval_tagger.py`。
   验收靠「跑脚本 + 手工打开页面看」。

4. **要花钱**：每跑一遍整套卷的打标约 12 万 token（`glm-4v-plus`），
   按量级算约 ¥1/套；换 flash 约 ¥0.3/套。

---

# 变更记录（按时间顺序）

---

## v1.0 — 2026-09-16 · 基线：项目总验收通过

DESIGN §7 的端到端清单**逐项实跑**（不是读代码判断），全程两条通道：
真实 HTTP API + 无头 Chrome（真点击、真渲染读数）。四个 view 数字一致；无 key 时
手动模式可完整走通。**此时项目功能完整可用。**

从那以后的所有改动记在下面。

---

## v1.1 — 2026-09-16 · 首页「已上传的卷子」汇总 + 删除单题

用户验收后提的两个需求，都是「用起来才发现缺的」。

### 新功能 1：首页卷子汇总面板

**是什么**：首页多一块「📊 已上传的卷子」，把传过的卷子按
**科目 → 年份 → 考试类型 → 城区**排成缩进目录树，每个节点带套数/题数/错题数/已归档数，
点某一行就跳到复核页并选中那套卷。

**动了哪里**

| 文件 | 改动 |
|---|---|
| `backend/api.py` | 新增 `GET /api/exams/summary?subject=`。**只输出真实存在过的分支**——没传过的年份/区不出现空行 |
| `static/home.js` | **新建**。渲染概览数字 + 缩进树 + 每行一条单色量级横条；点卷子行跳复核页 |
| `static/home.css` | **新建**。树/横条/层级样式 |
| `static/index.html`、`static/style.css` | 加 `#home-summary` 容器，引入上面两个文件 |
| `README.md` | 补充说明 |

**为什么这么设计**：横条是**单系列单色**，透明度**只随层级变、不随数值变**——
也就是说颜色不承载任何数据，数值由条长 + 右侧文字列双重给出，不依赖颜色也能读全
（对色觉障碍友好）。用缩进树而不是饼图/矩形树图，因为用户要的是「按目录排列 + 看总量」，
不是占比或趋势。

### 新功能 2：删除单题

**是什么**：大图弹层里多一个「🗑 删除此题」，二次确认（弹窗写明是哪套卷哪一题、会删文件、
不可恢复）后删除。

**动了哪里**

| 文件 | 改动 |
|---|---|
| `backend/api.py` | 新增 `DELETE /api/questions/{id}`：删题目行 + 主次知识点标签 + 新知识点建议 + **切片图片文件** |
| `static/app.js` | 大图弹层加删除按钮与确认流程；`LB_ITEM` 记住当前题 |

**安全处理**：只删项目目录内的文件，`image_path` 若在项目外（T2 的 CLI 允许传项目外目录）
则**跳过不删**。

**实测**（真实数据 + 无头 Chrome 真点击）：题库「共 39 题」→「共 38 题」，
首页汇总「86 道题 / 46 已归档」→「85 道题 / 45 已归档」；弹窗点「取消」→
**弹层保持打开、卡片数不变**（无副作用）；重复删除 → 404；切片确实从 `archive/` 消失
且**只删该题那一张**。

**设计取舍**：删除是**硬删除，没有回收站**。已用「弹窗写明是哪套卷哪一题 + 不可恢复」
兜住误操作。若日后要后悔药，最小改动是改成移到一个 `archive/_已删除/` 目录而不是真删。

---

## v1.2 — 2026-09-16 · 数学卷一律 21 题 + 删除整套卷

### 规则修正：数学卷不存在第 22 题

**背景**：用户确认「所有数学的卷子都只有 21 题，不会出现第 22 题」，
多出来的题「我自己看着办」。

**动了哪里**

| 文件 | 改动 |
|---|---|
| `backend/pdf_slicer.py` | 新增常量 **`EXPECTED_MAX_DEFAULT = 21`**（原默认 22）；`_plan` 里**尾部缺口不再建成「未定位」空题**，改为只出一句警告 |
| `backend/api.py` | `POST /api/exams` 的 `expected_max` 默认改取该常量 |
| `DESIGN.md` | §2 决策表新增第 14 条；§5 schema 注释同步 |
| `tests/test_pdf_slicer.py` | 新增 5 条断言覆盖新规则 |
| `README.md` | 复核页说明改写 |

**「多出来的题我自己看着办」怎么处理的**——分两种情形，**都不静默丢数据**：

1. **推测出来的空位（尾部缺口）→ 直接不建题。** 那些位置根本没有内容可切，
   建出来就是幽灵题。真实卷实测过：多出一个「第 22 题」，无切片、无知识点，
   还会以空标签混进题库和气泡图的统计里，首页数字全被它污染。
2. **真的锚到了第 22 题（后面确有内容）→ 保留 + 警告，不删。**
   **宁可多留一道让人扫一眼，也不误删一道题。**

**实测**：真实卷默认 21 → `located=21 total=21 gaps=[] warnings=无`，完全干净；
合成 22 题双栏卷 expected_max=21（超惯例）→ 保留 22 题 + 警告；
expected_max=30（欠惯例）→ 题号止于 22、**不生成 23~30 的幽灵题** + 警告。
库里已存在的那道幽灵题（exam#1 题 22）已按新规则删除 → `v_pool` 19→**18**、
`without_kp` 1→**0**（气泡图顶部黄条随之消失）。

### 新功能：删除整套卷

**是什么**：首页每行卷子右侧加一个 **🗑**（默认透明，鼠标悬停该行才显形，
触屏设备常显），二次确认（弹窗写明年份/区/类型/文件名/题数/错题数）后整卷删除。

**动了哪里**

| 文件 | 改动 |
|---|---|
| `backend/api.py` | 抽出公共函数 `_remove_slice_file()` 和 `_purge_questions()`（`DELETE /api/questions/{id}` 改为复用）；新增 `DELETE /api/exams/{exam_id}`：删该卷全部小题 + `storage/crops/{exam_id}/` + 残留源 PDF，并**逐级清理归档里因此变空的目录** |
| `static/home.js` | 每行的 🗑 按钮 + 确认流程；按钮点击 `stopPropagation()`，不会误触发「点行去复核页」 |
| `static/home.css` | 行网格加第 6 列给操作按钮 |
| `README.md` | 新增 FAQ「整卷传错了怎么办」 |

**实测**（真实 UI，无头 Chrome 真点击）：上传一套测试卷 → 首页统计从
「2 套卷 / 42 题 / 5 错题」变为 **「1 套卷 / 21 题 / 3 错题」**，该行消失，
复核页试卷下拉同步；文件系统里该卷文件全部消失、**空目录被逐级清掉**；
**exam#1 的数据分毫未动**；`DELETE /api/exams/999` → 404。

### 回归

`tests/test_pdf_slicer.py` 全部通过（含新增 5 条断言）；
`tests/test_bubbles.py` **19 PASS / 0 FAIL**；`tests/test_weekly.py` 全部 PASS。

> ⚠️ 为验证删整卷，本轮又上传了一套测试卷并**真实跑了 AI 打标**（约 ¥1、12 万 token）。
> 验证完已整卷删除。**这说明每次真实验证的代价是钱，动手前要想清楚。**

---

## v1.3 — 2026-09-16 · AI 打标质量优化（本项目最花钱、也最波折的一段）

这一段是 T4（AI 打标）接上真实 API 之后的连续攻关。按 `PROGRESS.md` 的 D~H 节整理。

### 第一步：接上真实 key，暴露并修掉 4 个 bug

在此之前所有验收都走「无 key 手动模式」或 mock，**真实调用一开就炸出 4 个问题**：

| # | 问题 | 现象 | 修法 |
|---|---|---|---|
| 1 | **依赖冲突（会直接崩）** | `openai 1.35.13` + `httpx 0.28.1` → 构造客户端就报 `TypeError: unexpected keyword argument 'proxies'` | `requirements.txt` 钉 `httpx==0.27.2`。**此前无 key 走手动模式永不触达这段代码，属潜伏 bug** |
| 2 | **key 无效时整卷被误锁** | 旧逻辑逐题吞掉报错 → 22 题全被标成「已完成」但内容是空的 → **卡片锁死，用户看到「识别完成」而不是「你的 key 是坏的」** | 区分 auth/网络等系统性失败，**立即中止整卷**（不再逐题烧钱），题目保持未打标，错误进前端红框 + 一键重试 |
| 3 | **9 类题型从没真正给过模型** | system prompt 只在「判定规则」里隐含提到，从没作为**显式清单**下发 → 模型答出「概率与统计」（那是知识点大类名，题型里叫「概率统计」，不带「与」字） | prompt 里显式列出 9 类 + 明确「题型 ≠ 知识点大类」 |
| 4 | **模型改写中文路径** | 要求精确到叶子后，模型开始**编造**叶子名（答「集合与交集」，树里是「交集、并集、补集运算」） | **给知识点树每行加编号，让模型报编号**——数字不会抄错。加了编号后**失败率从 8/22 → 0/22** |

**这段的教训**：任何只靠 mock 验收的东西，都可能在真实调用时崩。这直接催生了
下面「评测基建」那一节，以及本文件的 [[report-real-results]] 规矩。

**真实 token 用量**（`glm-4v-plus`，22 题一套）：
`prompt = 98,291  completion = 1,142  total = 99,433`，≈ 4,520 tokens/题、99k tokens/套。

### 第二步：建评测基建（从此改动可量化）

**为什么**：在此之前，任何 prompt/模型/参数的改动都无法证明是变好还是变坏，只能靠感觉。

| 文件 | 用途 |
|---|---|
| `tests/eval_tagger.py` | **新建**。三个子命令：`template`（导出人工标注模板）/ `dump`（从数据库导出当前模型的预测）/ `score`（打分）。指标：题型准确率、主知识点**叶子级**（最严）、**大类级**（宽松）、弃权率、按标注把握度分层 |
| `static/eval.html` | **新建**。标注页（开发用，不影响产品界面），左边切片图、右边真值下拉，AI 判定单列只读，实时算分，本地续标，可导出 JSON |
| `data/gold/gold_exam1.json` | **新建**。金标准集（**我据题干文字草拟，用户尚未复核**）：21 题，把握度 high=13 / mid=7 / low=1 |
| `data/gold/pred_exam1*.json` | 各版本模型的预测存档，可作后续对比基准 |

**最重要的诊断**（决定后面所有优化的方向）：
把 10 道判错题拆开看，**6 道是「大类判对、叶子判错」**，
即模型 **81% 能判对板块，但进了板块之后分不清是哪片叶子**。
它缺的不是「看懂题」，是「在 97 个近义叶子里的分辨力」。

### 第三步：据此做准确率优化

实现了四件事，每次都用同一份金标准打分，数字直接可比：

1. **两级分类（默认开启）**：第一步只让模型在 10 个板块里选 1，
   第二步**只给该板块的 8~13 个叶子**选 1。97 选 1 → 10 选 1 + ~10 选 1。
   这是针对上面那条诊断下的药。
2. **结构先验**：把「第 N 题 → 卷面形式」写进 prompt。
   ⚠️ **实测教训：作为软提示写进 prompt，模型并不遵守**（题 14/16 仍判错）。
3. **题型确定性校正**（软提示无效后的补救）：客观题段（1-10 选择 / 11-16 填空）
   **硬校正**为该段形式；解答题段（17 起）题型**由主知识点所属大类推出**。
   收益：既修掉错题，也让「错题本文件夹」与「气泡图板块」**构造上不可能矛盾**
   （修正前模型会自相矛盾：知识点判「函数与导数/切线」却把题型写成「解析几何」）。
   ⚠️ **这条规则是那 100% 题型准确率的来源，动它要格外小心**（见「待办 1」第 5 点）。
4. **允许弃权**：任何一步给出空值/越界编号/板块对不上，都判**弃权**并留空待手动补，
   不再硬塞一个错标签。**错标签比空标签危害大**——气泡图只数主标签，错标签会永久污染统计。

**结果（exam #1，21 题）**

| 指标 | 优化前 | 优化后 |
|---|---|---|
| **题型准确率** | 18/21 = 85.7% | **21/21 = 100%** |
| **主知识点 叶子级** | 11/21 = 52.4% | **13/21 = 61.9%**（高把握子集 84.6%） |
| **主知识点 大类级** | 17/21 = 81.0% | **19/21 = 90.5%** |
| token/套 | 99,433 | 121,305（+22%） |

### 第四步：模型 A/B + 「额度不足」中止与弹窗

**模型可用性实测**（`/models` 接口**只列文本模型**，视觉模型全都不在列表里但实际可用，
所以只能一个个试）：

- `glm-4v-plus` ✅、`glm-4v-flash` ✅（更便宜更弱）、`glm-4.6v` ✅（更差更慢）、
  `glm-4.5v` ✅
- **`glm-4.5-air` / `glm-4.6` ❌ 不能识图**（报 `content.type` 参数非法，只接受 text）
- `glm-4.6v-flash` 常年 429 限流

**`glm-4.6v` A/B 结论：不换。** 省一半钱，但四项质量指标全降、还慢 4 倍（2:09 → 8:44）。

**余额查询：做不了。** 实测 8 个候选接口路径（`/account/balance`、`/balance`、
`/user/balance`、`/billing/balance`、`/account`、`/usage` 及 v3/v2 版本）**全部 404**——
厂商没有公开的余额 API，只能在控制台看。

**所以改成「额度不足时中止 + 弹窗」**（用户明确要求）：
新增 `quota` 错误类别（429/402/余额/欠费/限额等）并**排在 auth 之前判**；
任一类系统性失败**立即中止整卷**。

**修掉的真缺陷**：原来 429 被归为「其他」，会**被逐题重试 22 遍**（既烧请求又拖时间），
而且失败会被吞成「已完成但空标签」→ **锁卡**，用户看到的是「识别完成」而不是「你欠费了」。

**实测**（用真的会 429 的 `glm-4.6v-flash` 当替身）：错误分类单测 7/7 通过；
整卷中止行为 `done=1/20` 即停（不是 22 次徒劳重试），**22/22 题全部保持未打标、被误标的 0 题**；
前端弹窗用 Node 直接跑真实源码验证，**9/9 断言通过**。

**前端改动**：`static/app.js` 按错误类型弹不同文案的窗口 + 红框 + 一键重试
（`POST /api/exams/{id}/retag`）；用指纹去重防止轮询变连环弹窗。

### 第五步：降本专项 —— 挖到一条关键计费事实 + 两个真 bug

**① 最关键的事实：图片按张固定收费，完全不看尺寸。**

拿同一张真实切片缩到 6 种尺寸、外加纯色图实测：

| 送进去的图 | 计费 token |
|---|---|
| 1058×1128 真实题目 | 2366 |
| 480×512 | 2366 |
| **64×64 纯白图** | **2366** |
| 1024×1024 纯白 | 2366 |

→ **缩图、裁空白、压缩字重，全都白费**。唯一有效的是**少发图**。
各模型「每张图」的价格差 13 倍：`glm-4v-plus` ≈2366 / `glm-4v-flash` ≈178 / `glm-4.6v` ≈125。
单题 token 构成：**两张图 4,732 + 全部文字约 800 → 图占 85%**。

**② 用户提的「整卷拼一张长图一次识别」方案实测：成本极优但不可用。**

| 做法 | tokens/套 | 省 | 题型 | 板块级 | **叶子级** |
|---|---|---|---|---|---|
| 现在（每题一图 × 22 题） | 121,338 | — | 100% | 90.5% | **61.9%** |
| **整卷一次调用** | **12,065** | **90%** | **100%** | 57.1% | **0%** |
| 分 3 批（每批 7 题） | 18,102 | 85% | 66.7% | 58.3% | 0% |

模型**确实看得清长图**（单独问时能答对），但**一次让它吐 21 题输出就退化**：
出现重号、漏题、把无关题判成同一个叶子。**根因是长条图里「内容↔题号」的对应关系不稳，
而不是图看不清。**

**结论：整卷长图是个极好的「题型判定器」（100%），但不能用来定知识点。**

**③ 捞到的可用优化：阶段1 可以换便宜模型**（阶段1 只是「10 个板块选 1」的粗活）。

| 配置 | tokens/套 | 叶子级 | 大类级 | 耗时 |
|---|---|---|---|---|
| `glm-4v-plus` + `glm-4v-plus`（**当前默认**） | 121,338 | **61.9%** | **90.5%** | **2:09** |
| `glm-4.6v`(阶段1) + `glm-4v-plus`(阶段2) | 83,621（省 31%） | 57.1% | 81.0% | 4:31 |
| `glm-4v-flash` 全程 | 37,233（省 69%） | 47.6% | 76.2% | 0:55 |

已加 `VLM_MODEL_STAGE1` 环境变量支持阶段1、阶段2 用不同模型。
⚠️ 注意只有 21 道题，**1 道题就是 4.8 个百分点**，表里 1~2 题的差异**在噪声范围内**。

**④ 顺带修掉的两个真 bug（接 `glm-4.6v` 才暴露）**

- **客户端超时写死 60 秒**：`glm-4.6v` 是推理模型、单次要 100 秒以上，60 秒会把整卷打断。
  已改为可配 `VLM_TIMEOUT`（默认 180 秒）。
- **没设 `max_tokens`**：推理模型的**思考过程也吃输出预算**，给太小会出现
  「推理把预算吃光、正文返回空串」（实测 `max_tokens=120` 时回答为空）。
  已加 `VLM_MAX_TOKENS`（默认 2000；`glm-4v-plus` 上限是 2048）。

### 这一段涉及的配置文件

- **新建 `.env`**（不入库、权限 600）：`VLM_BASE_URL` / `VLM_MODEL` / `VLM_API_KEY`。
  换模型只改这里，不用动代码。
- `requirements.txt`：钉 `httpx==0.27.2`。

### 这一段没做到的（如实记录）

- **知识点叶子级 61.9%（高把握子集 84.6%）仍不够无人值守。**
  剩余 8 道错题：题 5/10/14/15/16/17/20/21。
- **金标准是我草拟的，用户尚未复核**，所以 61.9% 是**下限而不是定论**。
- DESIGN §6-T4 的验收线「5 题人工比对合理」**未达到**（约 3/5 合理）。

---

## 未发布 — 2026-09-17 · 知识点库自定义 + 判别线索（**规划完成，一行代码未动**）

### 用户提了什么

1. 「题型判断很可以了，但你预设的知识点不太准，我想自己录入预设好的知识点你来进行判断」
2. 「把每题先识别成文字再给 AI 去判断题型，这样是不是可以更加准确？」

### 第 2 个想法：调查后决定**不做**，理由有三条实测支撑

1. **题型已经是 100%**，没有提升空间。
2. **文字早就喂给模型了**。`raw_text` 是用 PyMuPDF 从 PDF 文字层真实抽取的，
   两级流程的**每一步都同时带图和文字**。再 OCR 一遍只是把同样的东西重做。
3. **这份文字在数学卷上恰恰是最烂的部分**。真实样例：
   - 第 2 题题干 `ab<0` **整段丢失**，四个选项顺序全乱；
   - 第 4 题的 `±2.45` 抽出来变成两个都写 `2.45`，下标 `x₁` 变成 `x ，1`；
   - 第 19 题的三列表格**彻底崩塌**成一串孤立数字。

   中文叙述部分质量不错，但**数学符号、上下标、不等号、分式、表格基本都坏了**——
   而这正是判断知识点需要的信息。数学公式 OCR 是公认难题，新增 PaddleOCR 之类
   重依赖也只是把坏的东西再做一遍。

**真正卡住的是叶子级分辨力，不是「看不懂题」。** 所以力气全放在判别线索上。

### 第 1 个想法：方案已定稿，待实施

完整方案在 `/Users/yao/.claude/plans/indexed-napping-dragon.md`。

**用户已确认的四个选择**：
1. 录入方式 = **粘贴一份清单就行**（不做逐条编辑界面）
2. 旧数据 = **整棵替换，同名的保留**
3. 层级 = **大类 + 叶子两层**
4. 判别线索 = **要，用户愿意填**

**规划中确认的关键技术判断**（实施时别重新纠结）：
- 不扩 `origin`/`status` 的 CHECK 约束，改用独立的 `disabled` 列
  （扩 CHECK 要重建表，而这张表被错题本/气泡图/周报全依赖，风险不值）
- 判别线索**只进阶段2，不进阶段1**（阶段1 只选板块，线索无从发挥，还会推高成本）
- 大类加 `qtype_hint` 列保住那 100% 的题型准确率（否则用户改大类名会**静默失效**）
- 导入用「同名复用保留原 id」的方式，**不是先删再插**（先删再插会被外键直接拒绝）
- 被删但有题目引用的知识点改成**「停用」不删**（真删了周报会静默缩水、气泡图对不上）
- 必须先做 dry-run 预览：**改大类名等于断掉旧统计**，这件事要让用户当场发现

**顺带发现的现状问题**（也是待办）：主数据库 `storage/app.db` 现在是空的，
真正的 exam #1 数据在 `storage/test_weekly/real.db` 且图片路径全是死链。
原卷 PDF 和 21 张切片都还在，可以恢复。

### 本次同时建立的

- `CHANGELOG.md`（本文件）
- Claude 的长期记忆：用户的四条固定要求（非技术背景 → 用大白话；
  按窗口圈定范围；必须贴真实结果、mock 要标明；维护本文件与 `PROGRESS.md`
  并记清「完成/未完成/还差什么」）
